Sources for the plugin `plugin.appodeal.beta.AmazonAds`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.beta.AmazonAds" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
